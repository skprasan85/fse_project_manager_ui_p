import { ProjectViewPipe } from './project-view.pipe';

describe('ProjectViewPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectViewPipe();
    expect(pipe).toBeTruthy();
  });
});
